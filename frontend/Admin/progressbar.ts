import React from "react"
import styled from "styled-components"

const Wrapper = styled.div`
  width: 400px;
`

const BarOutlineWrapper = styled.div`
  width: 100%;
  padding-right: 50px;
  margin-right: -55px;
  display: inline-block;
`

const BarInlineWrapper = styled.div``
const BarSpanWrapper = styled.div``

export const ProgressBar = props => {}
